"""Interpolation helpers for part B."""

from ai_server.schemas import TrackPoint


def fill_missing_track_points(history: list[TrackPoint]) -> list[TrackPoint]:
    """Return the original history until interpolation logic is implemented."""

    return history
